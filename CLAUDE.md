# 旅索（タビサク）プロジェクトメモ

国内外の主要都市・空港・旅行系クレジットカードを検索できる旅行情報データベース。レキサク（歴索）と同じ構成（静的HTML＋Vanilla JS＋Supabase CDNクライアント、ビルドなし、Vercel直接デプロイ）で構築。

## 技術構成

- フロント: 静的HTML + Vanilla JS（フレームワーク・ビルドツールなし）
- DB: Supabase（レキサク等と共有のプロジェクト、project ref: `sunxpjbdvidftukiutxg`）
- デプロイ: Vercel（`vercel.json`はキャッシュ無効化ヘッダーのみ、ビルド設定なし）
- 本番URL: https://tabisaku.vercel.app
- GitHub: https://github.com/UKTOSHI/tabisaku

## ページ構成

| ファイル | 内容 |
|---|---|
| `index.html` | ランディングページ |
| `cities.html` | 都市 検索・一覧（国内外、都道府県・タグフィルタ） |
| `airports.html` | 空港 検索・一覧（国・タグフィルタ） |
| `cards.html` | 旅行系クレジットカード 比較・一覧（発行会社・年会費ソート） |
| `admin-x8k2qz.html` | 管理画面（都市/空港/カード/タグのCRUD、要ログイン）。2026-09-21に`admin.html`から改名し、全ページのnavリンクも削除して非公開URL化。⚠️本リポジトリはPublicなので、GitHub上でファイル一覧やコミット履歴（改名前は`admin.html`だった）を見れば誰でもこのファイル名にたどり着ける。URL秘匿はあくまで簡易的な効果に留まり、本質的な防御はSupabase Authのログインゲート |

## Supabaseテーブル

すべて`ts_`プレフィックス。レキサク等の既存テーブルとは完全に分離（データ共有なし・都道府県マスタも独自に`ts_prefectures`を新設）。

- `ts_prefectures` — 都道府県マスタ（旅索専用）
- `ts_tags` — タグマスタ（`content_type`: `city` / `airport` / `credit_card`）
- `ts_cities`（`region`列：海外都市のみ`北米`/`南米`/`アジア`/`ヨーロッパ`を設定、国内はnull／`food_info`・`spots_info`列：おすすめグルメ・観光スポットのテキスト（`spots_info`は①②③④の連番形式、読みにくい漢字地名は`<ruby>`タグでルビ付き・cities.html側はエスケープせず生HTMLとして描画）／`price_hotel`・`price_meal`・`price_transport_label`・`price_transport_value`列：物価目安を表形式で表示するための構造化フィールド（"2026年9月頃の目安"として提示、旧`price_info`列は未使用のままDBに残存）／`flight_time_from_tokyo`・`time_diff`列：東京からのフライト時間・日本との時差（海外都市のみ、国内はnull）。いずれもadmin画面のフォームに項目あり） / `ts_city_tags`
- `ts_airports` / `ts_airport_tags`
- `ts_credit_cards` / `ts_credit_card_tags`

全テーブルにRLSを適用：`anon`ロールは読み取り専用、`authenticated`ロールのみ書き込み可（レキサクと同じGRANT/RLSチェックリストを踏襲）。

## 管理画面ログイン

`admin-x8k2qz.html`はレキサクと同じSupabase Authの管理者アカウント（`admin@rekisaku.com`）を再利用してログインする（同一開発者のプロジェクトのため共用）。

---

## 🚧 現在の作業

**2026-09-21にTOP画面の階層検索・都市データの大幅拡充・都市詳細モーダルの再三のデザイン改善・管理画面URL非公開化まで実施、main反映済み。ワークフローが変更になり、この日から「ブランチを作らず直接mainにpush」が標準運用（ユーザー指示「修正はメインにこれからやって」「ブランチとかつくらないでよい」）。旧ブランチ`claude/top-search-hierarchy-49e9a1`はmainにfast-forwardマージ後、ローカル・リモートとも削除済み。コード変更あり（`index.html`/`cities.html`/`admin-x8k2qz.html`/`robots.txt`/`README.md`）・DB変更あり（`ts_cities`に`region`/`food_info`/`spots_info`/`price_hotel`/`price_meal`/`price_transport_label`/`price_transport_value`/`flight_time_from_tokyo`/`time_diff`列追加、国内1+海外56の計57都市に拡充、旧`price_info`列は未使用のまま残存）。都市詳細モーダルは色分けセクション→全画面化→物価/観光スポットの行分け→情報を2グループ化してアイコン付与→物価を表形式化・観光スポットに`<ruby>`ルビ、と複数回にわたりユーザーフィードバックを受けて改善を重ねた。次の宿題：①admin画面の`region`欄はフリーテキストのため、国名との対応が手打ち任せ（バリデーションなし）②物価情報は実測ではなくAIによる目安値なので、時期が経つほど実態とズレる可能性がある③ルビ付きの`spots_info`は生HTMLとして描画しているため、admin画面で編集する管理者は`<ruby>`タグの書き方を知っている必要がある（バリデーションや入力補助なし）。**

- ✅ **旅索（タビサク）初期構築（完了・main反映済み 2026-09-20）**: ユーザー依頼「レキサクと同じシステム構成で、日本国内・海外の旅行情報（都市・空港・クレジットカード）を検索できるDBサイトを作って」に対応
  - Supabase共有プロジェクト（`sunxpjbdvidftukiutxg`）に`ts_`プレフィックスの新規テーブル群を作成。レキサクの`prefectures`は再利用せず、ユーザー指示「データは分ける」に従い`ts_prefectures`を独自に新設してデータ完全分離
  - 47都道府県データ、動作確認用サンプルデータ（都市3件：東京・上海・ニューヨーク、空港2件：成田・羽田、カード2件：ANA/JALゴールドカード）を投入
  - `index.html`/`cities.html`/`airports.html`/`cards.html`/`admin.html`を新規作成。レキサクのspots.html（全件フェッチ＋クライアント側フィルタ、タグチップ）・admin.html（Supabase Auth・delete→insert方式のタグ同期）のパターンを踏襲
  - ローカルで静的サーバー（`npx serve`）を立てて全ページの動作確認（Supabaseからのデータ取得・検索フィルタ・モーダル詳細表示・admin.htmlのログインゲート）を実施、コンソールエラーなしを確認
  - GitHubリポジトリ`UKTOSHI/tabisaku`を新規作成しpush→Vercelにインポートしてデプロイ、本番URL`https://tabisaku.vercel.app`で動作確認済み
  - 管理者アカウントはレキサクと同じ`admin@rekisaku.com`を再利用する方針（実際のパスワードでのCRUD往復確認はユーザー自身が今後実施）

---

## 📝 セッションログ

### 2026-09-21（TOP画面の階層検索・cities.htmlのプルダウン検索を実装）コード変更あり・DB変更あり（`ts_cities.region`列追加＋海外都市27件投入）・コミット/push実施
- ユーザー依頼「TOP画面の検索はまず、国内旅行、海外旅行から入る。海外旅行の場合は北米、南米、アジア、ヨーロッパのようにして、そのあと主要国、都市を選べるように。まずメジャーな国と都市（日本人がよく行くところ）を入れていく」に対応
- **DB変更**: `ts_cities`に`region`列（text）を追加。既存の上海=アジア、ニューヨーク=北米を設定。日本人に人気の海外主要都市27件（北米5：NY・LA・ハワイ・ラスベガス・バンクーバー／南米3：リオデジャネイロ・リマ・ブエノスアイレス／アジア10：ソウル・台北・上海・北京・香港・バンコク・シンガポール・ホーチミン・バリ島・セブ／ヨーロッパ9：パリ・ローマ・ロンドン・ベルリン・バルセロナ・チューリッヒ・アムステルダム・アテネ・プラハ）を`description`/`best_season`/`timezone`/`currency`/`language`/`visa_notes`/`access_info`込みで投入
- **`index.html`**: TOPページに階層型検索パネルを新設。「国内旅行／海外旅行」→地域ボタン→国ボタン（都市数表示）→都市ボタンの順に進む構成。データはSupabaseから動的取得のため、`admin.html`経由で追加された都市も（`region`が設定されていれば）自動的に選択肢へ反映される。パンくずでいつでも前段階に戻れる
- **`cities.html`**: TOPからの遷移用に`?domestic=1`/`?country=国名`/`?q=都市名`のURLパラメータで初期絞り込み状態を復元できるよう対応。加えてユーザーから「国名・都市名は入力ではなく選択式に」との追加依頼を受け（AskUserQuestionで対象画面をcities.htmlの絞り込みと確認）、既存の都道府県プルダウンと並ぶ形で「国」（地域でoptgroup化）と「都市」のプルダウンを追加。都市プルダウンは現在の絞り込み条件（国内/海外・都道府県・国・タグ）に応じて中身が連動し、選択すると該当都市の詳細モーダルを直接開く
- **動作確認**: ローカル静的サーバーでTOPの地域→国→都市の階層遷移、パンくず、cities.htmlの国・都市プルダウン連動、URLパラメータ復元を確認。コンソールエラーなし
- **既知の課題（対応済み、下記参照）**: 上記の「admin.htmlにregion項目が無い」課題は、同日中にadminフォームへの`region`テキスト項目追加で解消（完全自動化ではなく手入力）
- **ワークフロー変更**: 当初この機能はfeatureブランチ→PR作成の流れで進めていたが、ユーザーから「修正はメインにこれからやって」「ブランチとかつくらないでよい」との指示を受け、以降はブランチを作らず直接mainにpushする運用に変更。既存のfeatureブランチはmainにfast-forwardマージ後、ローカル・リモートとも削除（リモート削除はauto modeのBash分類器にブロックされたためユーザーに手動実行を依頼）
- **都市データの絞り込みと拡充（ユーザー依頼「国はもっと絞ってよい、その分メジャーな観光国の都市を増やして。国は半分くらいに」）**: 海外の国数を23→13に削減（ブラジル・アルゼンチン・ベトナム・インドネシア・フィリピン・ドイツ・スイス・オランダ・ギリシャ・チェコの10都市を削除）、代わりにアメリカ・カナダ・ペルー・韓国・台湾・タイ・フランス・イタリア・イギリス・スペインの都市数を増やす形で再構成（計27都市を維持）。シンガポール・香港は都市国家的性質のため1都市のまま据え置き
- **さらに追加依頼（「中国に重慶とか都市をもっと倍に」「各国の都市を倍にして」）**: 中国に重慶・西安を追加（2→4都市）した後、全13カ国の都市数をそれぞれ倍増（シンガポール・香港を除く）。最終的に海外都市は27→56件、東京と合わせて全57都市に
- **観光情報の充実（ユーザー依頼「物価も入れて（2026/9時点として）、おすすめの食事、観光スポットも充実させて」）**: `ts_cities`に`price_info`（物価目安：ホテル/食事/タクシーの円換算レンジ、「【2026年9月頃の目安】」と明記）・`food_info`（おすすめグルメ）・`spots_info`（観光スポット3〜4件）の3列を追加し、全57都市分を作成。`description`（概要文）も全都市で名所・グルメ・体験を盛り込んだ2文構成に書き直し済み。⚠️物価情報はWeb検索等で実測したものではなく、一般的な相場感に基づくAIの目安値である旨に注意（ユーザーへの提示時も「目安」である点を明示）
- **`admin-x8k2qz.html`（管理フォーム更新）**: 上記3列と`region`列をフォームに追加（`region`は北米/南米/アジア/ヨーロッパ等を手入力するテキスト項目、バリデーションなし）
- **`cities.html`（モーダルのデザイン刷新）**: ユーザー依頼「都市の情報も色をつけたりデザインも工夫させて」に対応し、詳細モーダルを色分けされたセクション形式に変更（📍観光スポット＝緑、🍴おすすめグルメ＝オレンジ、💰物価の目安＝青の帯＋「2026年9月時点」バッジ、その他の基本情報はグリッド表示）。一覧グリッドの国・地域バッジも地域ごとに色分け（北米=青／南米=赤／アジア=オレンジ／ヨーロッパ=紫／国内=teal）
- **管理画面の非公開URL化（ユーザー依頼「管理者のログインは別のURLにして」）**: `admin.html`を`admin-x8k2qz.html`に改名し、全公開ページ（index/cities/airports/cards）のnavから「管理」リンクを削除、`robots.txt`の`Disallow: /admin.html`行も削除（robots.txtは誰でも読めるため、そこに非公開パスを書くとかえって居場所を教えてしまうため）。README.mdからも具体的なファイル名は削除し「非公開URL」とだけ記載。⚠️重要な注意点：本リポジトリ（`UKTOSHI/tabisaku`）はGitHub上でPublicなので、ファイル一覧や改名前のコミット履歴を見れば誰でも実際のファイル名にたどり着ける。URLを分かりにくくする効果は限定的で、本質的な防御は変わらずSupabase Authのログインゲート。より確実に隠したい場合はリポジトリをPrivateにする、または別のアクセス制限（Vercelのパスワード保護等）を検討する必要がある旨をユーザーに要説明（未実施）
- **フライト時間・時差の追加（ユーザー依頼「東京から飛行機で何時間か、時差はどうかも入れて」）**: `ts_cities`に`flight_time_from_tokyo`（東京からの目安飛行時間、直行便かどうか・経由地も明記）・`time_diff`（日本との時差、サマータイムがある地域はサマータイム期間の時差も併記）の2列を追加し、海外56都市すべてに設定（東京自身は対象外）。cities.htmlの詳細モーダルの情報グリッド先頭に表示、adminフォームにも項目追加。⚠️これも実測・API取得ではなく一般的な知識に基づく目安値（特にフライト時間は季節・就航状況で変動するため「約」表記）
- **モーダルの見やすさ改善（複数回にわたるユーザーフィードバック）**: ①「画面が小さくて全部表示できていない」→ 幅720px未満では詳細モーダルを画面いっぱいに表示するよう変更（720px以上は従来通り中央寄せカード）②「物価は1行でなく行を分けて」→ 物価（ホテル／食事／タクシー）と観光スポット（①②③④）を`／`・丸数字区切りでパースして`<li>`ごとに縦積み表示③「見やすさ重視して」→ 概要文を背景色付きボックスに、情報項目を「✈️旅のプランニング」（フライト時間・時差・ベストシーズン・タイムゾーン）と「📋実用情報」（通貨・言語・ビザ・アクセス）の2グループに分けて絵文字アイコンを付与、見出し階層とセクション間余白を強化④「物価は金額を揃えて表にする」→ `ts_cities`に`price_hotel`/`price_meal`/`price_transport_label`/`price_transport_value`の4列を新設し、物価を「項目名（左）・金額（右揃え太字）」の2列テーブルに変更。旧`price_info`（1本のテキスト列）は表示に使わなくなったが、データ自体はDBに残したまま⑤「観光スポットの漢字ばかりのところにフリガナ」→ 中国8都市・台湾4都市・韓国4都市（香港は元々カタカナ表記のみで対象外）の`spots_info`に`<ruby>`タグでルビを追加（中華圏は日本語の音読み慣用読み、韓国は現地発音のカタカナ表記という使い分け）。cities.html側は`spots_info`のみエスケープをやめて生HTMLとして描画するよう変更（admin経由の信頼済みコンテンツという前提）⑥「物価に現地通貨も」→ `price_hotel`/`price_meal`/`price_transport_value`のテキストに「円（現地通貨）」の形で現地通貨換算を追記（列追加はせず既存3列のテキストを書き換え）。通貨ごとに固定レート（例：USD=150円、EUR=163円、KRW=0.11円など、いずれも2026年9月頃のAIによる概算目安）で円建て金額から逆算。⚠️実際の為替レートではなく目安であることに注意

### 2026-09-20（旅索（タビサク）初期構築・Vercel本番デプロイまで完了）✅main反映済み・コード変更あり（新規プロジェクト一式）・DB変更あり（`ts_`系テーブル新規作成）
- ユーザー依頼「レキサクと同じシステム構成（Vercel+Supabase）で、日本国内・海外の旅行情報を検索できるものを作って。都市の情報・クレカの情報・飛行機の情報など、旅行に特化したデータベース。まずアスクアンドクエスチョンで作って。タイトルは旅索（タビサク）」に対応
- **要件確認（AskUserQuestion）**: 技術構成はレキサクと完全に同じ（静的HTML＋Vanilla JS＋Supabase CDN、ビルドなし）に決定。Supabaseは既存の共有プロジェクトを使うが、テーブル・データはレキサクと完全に分離する方針に決定（ユーザー「レキサクとテーブル（データは分ける）」の指示を受け、都道府県マスタも`prefectures`を再利用せず`ts_prefectures`を独自新設に変更）。クレカ情報＝旅行系クレカの比較コンテンツDB（個人のカード管理ではない）、飛行機情報＝成田・羽田など主要空港の情報、都市情報＝国内外の主要都市（上海・NY・台北等）と確定
- **設計**: Plan agentで詳細設計（スキーマ・ページ構成・実装順序・検証方法）を作成。レキサクの実ファイル（`admin.html`のSupabaseクライアント埋め込み・認証ゲート・delete→insert方式のタグ同期パターン、`spots.html`の全件フェッチ＋クライアント側フィルタパターン、`vercel.json`）を直接読んで踏襲パターンを確認
- **Supabase実装**: `sunxpjbdvidftukiutxg`プロジェクトに`ts_prefectures`/`ts_tags`/`ts_cities`/`ts_city_tags`/`ts_airports`/`ts_airport_tags`/`ts_credit_cards`/`ts_credit_card_tags`を作成。全テーブルにanon=SELECT/authenticated=ALLのRLSポリシーを適用（`get_advisors`でレキサク側の既存warning以外に新規の問題がないことを確認）。47都道府県＋動作確認用サンプルデータ（都市3・空港2・カード2）を投入
- **フロント実装**: `index.html`（ランディング＋注目都市の動的表示）、`cities.html`/`airports.html`/`cards.html`（検索・フィルタ・タグチップ・モーダル詳細）、`admin.html`（タブ切替式CRUD、都市/空港/カード共通のconfig駆動フォーム＋タグ管理タブ）を新規作成。`vercel.json`/`robots.txt`/`sitemap.xml`/`manifest.json`/`README.md`も作成
- **動作確認**: `.claude/launch.json`で`npx serve .`のローカルサーバーを起動し、Browserツールで全ページを検証。index/cities/airports/cardsでSupabaseからのデータ取得・フィルタ・モーダル表示が正常動作、admin.htmlのログインゲートが誤パスワードで正しく拒否されることを確認。コンソールエラーなし
- **Git/デプロイ**: ローカルでgit init→コミット。GitHubへの未ログインを検知したためユーザーにログインを依頼→ログイン後、Claude in Chromeで`github.com/new`から`UKTOSHI/tabisaku`（Public、空リポジトリ）を作成。ユーザーに都度確認（AskUserQuestion）を取ってから`git push -u origin main`（auto modeのBash分類器が最初のpush試行をブロック→ユーザーに再確認して実行）。続けてVercelにログイン済みのChromeセッションで`vercel.com/new`からGitHubリポジトリをインポートし、Root Directory`./`・環境変数なしでデプロイ。本番URL`https://tabisaku.vercel.app`（プロジェクト設定のDomainsタブで確認、デプロイ直後のハッシュ付きURLとは別）で動作確認済み
- **教訓**: git push・GitHubリポジトリ作成・Vercelデプロイのような共有システムへの操作は、事前にプラン承認を得ていても実行の都度ユーザーに確認を取るべき（auto modeのBash分類器もpushを自動ブロックする挙動だった）。また「本番URL」を聞かれた際、デプロイ直後にVercelが返すハッシュ付きURL（`tabisaku-xxxxx-uktoshis-projects.vercel.app`）は個別デプロイのプレビューURLであり、安定した本番ドメイン（`tabisaku.vercel.app`）はプロジェクト設定のDomainsタブで別途確認する必要がある
- ユーザーから「Claude.mdを作成して」の指示を受け、本ファイル（`CLAUDE.md`）をグローバル設定のプロジェクト一覧に倣った形式（技術構成・DBスキーマ・🚧現在の作業・📝セッションログ）で新規作成し、レキサクの`CLAUDE.md`の書式（現在の作業＝太字サマリ＋✅完了ログ、セッションログ＝日付見出し＋箇条書き）を踏襲。ローカルでコミット（`9ef4b42`）のみ実施し、GitHubへのpushは今回未実施（ユーザーが「終了」と言うたびに現在の作業・セッションログを更新する運用は今後も継続）
